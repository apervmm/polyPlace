services:
  api:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app   # live-reloads code changes without rebuilding the image
    command: uv run fastapi dev --host 0.0.0.0 --port 8000